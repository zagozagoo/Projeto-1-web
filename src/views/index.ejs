<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Controle</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>
    <link rel="stylesheet" href="css/style.css" />
</head>

<body>
    <header>
        <nav class="shadow-lg navbar navbar-expand-lg bg-body-tertiary top" style="background-color: #e3f2fd;">
            <div class="container-fluid">
                <a class="navbar-brand" href="/"><strong class="RGB_Controle">Controle</strong> de Estudantes</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>

            <div class="" id="navbarText">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <strong>Cadastrar: </strong>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/aluno">Alunos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/sala">Sala</a>
                    </li>
                </ul>
            </div>
        </nav>
    </header>
    <main>
        <div class="opcoes_salas">
            <span class="span_salas">Salas de Aula: </span>
            <form action="/", method="post">
                <select class="form-select" onchange="this.form.submit()" name="id">
                    <option value="padrao" <%= (selecionado == "padrao") ? "selected" : "" %>>Selecionar</option>
                    <% for (let i=0; i < salas.length; i++) { %>
                        <option value="<%= salas[i].IDSala %>" <%= (selecionado == salas[i].IDSala) ? "selected" : "" %>>
                            <%= salas[i].Nome %>
                        </option>
                    <% } %>
                </select>
            </form>
        </div>
        <h1>Alunos</h1>
        <div class="container">
            <% for (let i=0; i < alunos.length; i++) { %>
                <div class="card" style="width: 18rem;">
                    <img src="images/<%= alunos[i].Foto %>" class="card-img-top" alt="...">
                    <div class="card-body">
                        <h5 class="card-title"><%= alunos[i].Nome %></h5>
                        <p class="card-text"> Idade: <%= alunos[i].Idade %></p>
                        <a href="#" class="btn btn-primary">Editar</a>
                    </div>
                </div>
            <% } %>
        </div>
    </main>
</body>

</html>